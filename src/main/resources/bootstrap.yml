spring:
  application:
    name: ms-sidecar-node
  cloud:
    config:
      fail-fast: true
      enabled: true
      uri: http://localhost:8888

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVICE_URL:http://localhost:8761/eureka/}